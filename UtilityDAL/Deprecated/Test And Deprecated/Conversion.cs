namespace UtilityDAL
{
    internal class Conversion
    {
        //public static MutableDataTable ConvertToGPdotNetFormat(string path)
        //{
        //    MutableDataTable dt = DataTable.New.ReadCsv(path);

        //    //            var xx = dt.GetColumns("HomeTeam", "AwayTeam", "FTHG", "FTAG");

        //    //output
        //    dt.CreateColumn("Diff", CreateDiffColumn);

        //    // post match data i.e data that wouldn't be known beforehand (for predictions)
        //    foreach (string s in new string[18] { "FTHG", "FTAG", "FTR", "HTHG", "HTAG", "HTR", "HS", "AS", "HST", "AST", "HF", "AF", "HC", "AC", "HY", "AY", "HR", "AR" })
        //        try { dt.DeleteColumns(s); } catch { }

        //    return dt;

        //}
    }
}