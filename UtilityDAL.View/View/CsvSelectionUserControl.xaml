<UserControl x:Class="UtilityDAL.View.CsvSelectionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                xmlns:utility="clr-namespace:UtilityWpf.View;assembly=UtilityWpf.View"
             mc:Ignorable="d">
    <StackPanel Name="sp">
        <StackPanel.Resources>
            <ResourceDictionary Source="/UtilityDAL.View;component/View/SelectionContentControl.xaml" />
        </StackPanel.Resources>

        <!--<utility:DirectorySelectionControl   Name="folder" DockPanel.Dock="Top"  Directory="{Binding Directory,Mode=OneWayToSource}" />
        --><!--<ContentControl Grid.Row="1" Name="maincontent"  DataContext="{Binding }"  Content="{StaticResource  SelectionTemplate }" />--><!--
        <local:CsvDbControl Directory="{Binding Directory,Mode=OneWay}"></local:CsvDbControl>-->
    </StackPanel>
</UserControl>